systemLog:
  destination: file
  path: /dev/null
  quiet: true

storage:
  dbPath: /data/db

net:
  bindIp: 0.0.0.0
  ssl:
    mode: disabled  # Explicitly disable SSL to match your no-TLS preference
